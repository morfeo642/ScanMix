cmake_minimum_required(VERSION 2.8)
project( procesaImagen.cpp )
find_package( OpenCV REQUIRED )
add_executable( ../procesaImagen procesaImagen.cpp )
target_link_libraries( ../procesaImagen ${OpenCV_LIBS} )
